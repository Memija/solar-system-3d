import { describe, it, expect, vi } from 'vitest';
import { SceneManager } from './src/components/SceneManager';
import * as THREE from 'three';

describe('Camera Rotation Bug', () => {
    it('check if updateMeasurement gets called every frame and if rotation updates distance label position', () => {
        // ...
    });
});
